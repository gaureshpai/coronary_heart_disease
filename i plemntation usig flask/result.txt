{% extends "base.html" %}

{% block content %}
<div class="row">
    <div class="col-md-8 mx-auto text-center">
        <h2 class="mb-4">Prediction Results</h2>
        
        <div class="card {% if result.prediction == 1 %}border-danger{% else %}border-success{% endif %} mb-4">
            <div class="card-body">
                <h3 class="card-title {% if result.prediction == 1 %}text-danger{% else %}text-success{% endif %}">
                    {{ result.risk_level }} Risk
                </h3>
                <p class="card-text lead">{{ result.message }}</p>
                <p class="card-text">Probability: {{ "%.2f"|format(result.probability * 100) }}%</p>
            </div>
        </div>
        
        <div class="alert {% if result.prediction == 1 %}alert-danger{% else %}alert-success{% endif %}">
            {% if result.prediction == 1 %}
            <h4>Recommendations:</h4>
            <ul class="text-start">
                <li>Consult with a healthcare professional</li>
                <li>Consider lifestyle changes (diet, exercise)</li>
                <li>Monitor your blood pressure regularly</li>
                <li>Reduce or quit smoking if applicable</li>
            </ul>
            {% else %}
            <h4>Good News!</h4>
            <p>Your risk is currently low. Maintain healthy habits to keep it that way.</p>
            {% endif %}
        </div>
        
        <a href="/predict" class="btn btn-primary">Make Another Prediction</a>
        <a href="/" class="btn btn-outline-primary ms-2">Return Home</a>
    </div>
</div>
{% endblock %}